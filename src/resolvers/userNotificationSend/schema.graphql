type UserNotificationSendResponse {
  success: Boolean!
}

extend type NotificationsMutation {
  userNotificationSend(entityId: ID!, notificationTemplateId: ID!, filter: UserFilter): UserNotificationSendResponse
}
